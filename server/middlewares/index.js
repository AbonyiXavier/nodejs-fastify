// const { businessCheck } = require('../middlewares/business');
// const { verifyToken } = require('../middlewares/verify-token');

// module.exports = {
//   indexMiddleware: async (request, reply, { business, verify }, next) => {
//     try {

//       if (condition) {
//      await businessCheck(request, reply);
//       }

//       next(request, reply,);
//     } catch (error) {
//       console.log('error', error);
//     }
//   },
// };
